## 强制约束（必须遵守）
- 不检查、不修改 `data-analytics-notes\\docs` 下红框的四个目录：
  - `docs/PowerBI/**`
  - `docs/Projects/**`
  - `docs/Python/**`
  - `docs/SQL/**`

## 范围（只动“会参与站点构建/渲染”的其余部分）
- **纳入清理**：
  - 构建/配置：`mkdocs.yml`、`requirements.txt`、`.github/workflows/deploy.yml`
  - 主题覆盖：`overrides/**`
  - Docs 仅限以下路径：
    - `docs/index.md`
    - `docs/assets/**`
    - `docs/javascripts/**`
    - `docs/stylesheets/**`
- **排除**：`.venv/**`、`site/**`、`Data_and_original_files/**`，以及上述 4 个 docs 内容目录。

## 已发现的高置信“冗余/未使用”点（基于只读检查）
- 仓库没有 `package.json/vite/webpack/tsconfig` 等前端构建体系，也没有 `.ts/.vue/.jsx` 文件；站点构建主要依赖 MkDocs Material。
- `docs/javascripts/mathjax.js` 与 `docs/javascripts/mermaid-init.js` 未在 `mkdocs.yml` 的 `extra_javascript` 中加载，且全仓也没有引用；同时 `docs/javascripts/site.js` 已实现 Mermaid/MathJax 的按需加载与初始化逻辑，因此这两个文件属于高概率历史遗留冗余。

## 实施步骤（会改代码）
### 1) 建立“引用清单”并做安全删减（仅限允许目录）
- 以 `mkdocs.yml` 的 `extra_css/extra_javascript` 为入口，列出实际加载的 CSS/JS。
- 在允许范围内做引用扫描：`docs/index.md`、`overrides/**`、`docs/javascripts/**`、`docs/stylesheets/**`。
- 仅删除：**全仓无引用且不被 MkDocs/Material 主题隐式使用** 的文件与代码块。

### 2) JS 清理（优先处理冗余文件）
- 删除 `docs/javascripts/mathjax.js` 与 `docs/javascripts/mermaid-init.js`（它们当前未被加载/引用）。
- 保持 `docs/javascripts/site.js` 的按需加载逻辑不变。

### 3) CSS/HTML 进一步精简（保守策略）
- 对 `docs/stylesheets/custom.css` 与 `docs/index.md` 做一次“选择器/类名引用核对”，删除确定未使用的自定义选择器与无效样式。
- 主题内部类（`md-*` 等）默认保守保留，避免误伤主题行为。

### 4) 构建与回归验证（不触碰四个内容目录）
- 运行 `python -m mkdocs build --strict`，确保严格构建通过。
- 运行 `python -m mkdocs serve` 并抽查：
  - 首页（`docs/index.md`）
  - 不额外打开/检查 `docs/PowerBI、docs/Projects、docs/Python、docs/SQL` 下的页面。

## 交付物
- 一套更精简的“构建相关代码”（仅限允许目录），并给出删除/合并项清单与理由，确保站点正常构建与首页正常渲染。